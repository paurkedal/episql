"lib": include
<bin/*>: \
    use_episql, linkall
<{bin,lib,tests}/*>: \
    package(prime), \
    package(re), package(re.glob), \
    package(xmlm)
<lib/caqti_persist.*> or <tests/*_persist.*>: \
    thread, \
    package(caqti.lwt), \
    package(extunix), \
    package(lwt), \
    package(prime), package(prime.unstable), \
    package(react)
<tests/*_macaque.*>: \
    package(macaque)
<tests/*>: \
    use_caqti-persist, \
    package(calendar)
